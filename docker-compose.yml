version: '2'
services:
  web:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8000"
    volumes:
      - /usr/src/app/static

  nginx:
    restart: always
    build: ./nginx/
    ports:
      - "8070:8070"
    volumes:
      - /www/static
    volumes_from:
      - web
    links:
      - web:web
